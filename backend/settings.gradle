pluginManagement {
    repositories {
        maven { url "https://repo.maven.apache.org/maven2" }
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'backend'